{
  "name": "knowledge-mcp",
  "version": "1.0.0",
  "description": "A shared agent knowledge system with brain-inspired memory decay, associative linking, and knowledge graph visualization — exposed as an MCP server.",
  "type": "module",
  "bin": {
    "knowledge-mcp": "./build/index.js"
  },
  "scripts": {
    "build": "tsc && rm -rf build/graph/static && cp -r src/graph/static build/graph/static && chmod 755 build/index.js",
    "start": "node build/index.js",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "files": [
    "build"
  ],
  "keywords": [
    "mcp",
    "knowledge",
    "agent",
    "memory",
    "knowledge-graph"
  ],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.0",
    "better-sqlite3": "^11.0.0",
    "zod": "^3.25.0"
  },
  "optionalDependencies": {
    "@xenova/transformers": "^2.17.2"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.18"
  }
}
